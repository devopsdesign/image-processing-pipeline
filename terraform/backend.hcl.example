# For local runs only:  terraform init -backend-config=backend.hcl
# CI passes bucket + region as -backend-config flags instead (see deploy.yml).
bucket = "cloudsight-tfstate-000000000000"
region = "us-east-1"
